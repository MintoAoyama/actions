name: Push

on:
  push:
    tags-ignore:
      - '*'
    branches:
      - '*'

jobs:
  push:
    
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Tokyo

    steps:
        
    - name: echo
      run: |
        ls -all
